<%# Remove the norm row from the table %>
<%= turbo_stream.remove dom_id(@norm) %>

<%# Update flash messages container with success notification %>
<%= turbo_stream.update "flash-messages" do %>
  <%= render "partials/turbo_notification", message: t('norms.messages.deleted', name: @norm.name) %>
<% end %>