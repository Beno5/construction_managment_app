<%# Remove the machine row from the table %>
<%= turbo_stream.remove dom_id(@machine) %>

<%# Update flash messages container with success notification %>
<%= turbo_stream.update "flash-messages" do %>
  <%= render "partials/turbo_notification", message: t('machines.messages.deleted', name: @machine.name) %>
<% end %>